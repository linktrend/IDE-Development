# Migration catalog pointer

Canonical Wave 1 migration catalog lives at:

- `core/managed-core/migrations/catalog.json`
- schema: `core/managed-core/migrations/schema.json`

This `migration/` directory exists only as a stable discovery alias for layout docs.
Do not place a duplicate `catalog.json` here.
