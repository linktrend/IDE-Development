# Templates

These templates define the canonical structure for common planning and execution artifacts.

Use the smallest template that fits the task and adapt it instead of inventing ad hoc formats.
