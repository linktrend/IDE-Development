# Execute: Dispatch Install

Use this prompt when setting up or reviewing dispatch-oriented automation for the shared `.cursor` system.

## Goal

Document or implement the minimum viable automation path for planner, reviewer, integrator, and executor flows without relying on legacy `LiNKdev` files.

## Read First

1. `.cursor/workflows/dispatch-v2.md`
2. `.cursor/checklists/wire-checklist.md`

## Rules

1. Treat automation as optional infrastructure around the local `.cursor` system, not as its source of truth.
2. Keep labels, triggers, and role responsibilities explicit.
3. Separate verified automation from intended future automation.

## Required Outcome

- dispatch architecture is understandable from local files
- setup expectations are documented locally
- open infrastructure gaps are visible to the operator
