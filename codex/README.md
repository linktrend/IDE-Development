# Codex Adapter

This directory provides Codex-facing entrypoints for the shared AI development core.

Canonical content lives in `../core/`.

Compatibility content still resolves through `../.cursor/`.

Use this directory to understand how Codex should enter the system without changing the underlying doctrine or command surface.
