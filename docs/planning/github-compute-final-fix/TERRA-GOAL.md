# Terra `/goal` — IDE Development GitHub Compute Final Fix

Complete the permanent IDE Development GitHub Compute Final Fix end to end. Begin
in `/Users/linktrend/Projects/IDE Development` by locating the approved planning
commit containing `docs/planning/github-compute-final-fix/README.md`. Read, in the
binding order stated by that README, every top-level planning document and every
packet under `docs/planning/github-compute-final-fix/packets/`; treat those files
as the authoritative scope, frozen interfaces, dependency graph, emergency
authority, rollout procedure, and acceptance criteria. Read the repository root
`AGENTS.md`, perform the documented preflight, record planning commit `P0` and the
then-current `origin/development` as immutable implementation base `B0`, inventory
and preserve all dirty work, worktrees, branches, open PRs, rulesets, runners,
containers, services, custom App state, and the completely separate concurrent
feature, then create `phase/github-compute-final-fix` from `B0`. Do not begin Luna
execution until the funded GitHub account can start a minimal private
`ubuntu-24.04-arm` canary and packet path ownership is proven non-overlapping.

Act as GPT-5.6 Terra Medium orchestrator, integrator, verifier, release controller,
and final reporter. Dispatch GPT-5.6 Luna High executors only through Codex CLI,
up to three safely in parallel, using separate issue branches and worktrees and
the exact packet instructions. Maintain an attempt ledger. Verify every returned
diff and rerun its validations; integrate accepted exact commits serially into the
Phase branch with cumulative regression. Give a Luna no more than two attempts on
one packet; after two failures, take over and finish that packet yourself. Do not
use a separate auditor: verify that the work was actually done. Do not enter Wave
2 before Wave 1 combined PASS, do not release before Wave 2 combined, disposable-
consumer, migration, rollback, and live hosted-ARM64 canary PASS, and do not roll
out consumers before the exact source release is on IDE Development `main`.

Implement the approved permanent behavior: checkpoint commits and pushes start no
Actions; related issue commits are combined into one or few Phase PRs; GitHub-
hosted `ubuntu-24.04-arm` runs fast checks on the latest candidate; concurrency
cancels obsolete runs; Bugbot and one repository-owned full suite run only on the
sealed final exact candidate; one exact candidate receives at most two
infrastructure attempts and development stops and alerts after the second; no more
than two sealed candidate revisions occur without HOLD; successful full-suite
evidence is bound to repository, Git tree, dependencies, profile, workflow, run,
and evidence digests; unchanged development/staging/main content reuses that
receipt and runs only short receipt/source-policy gates; changed content fails
closed. Use least-privilege built-in `GITHUB_TOKEN` inside workflows and Carlos's
normal authenticated repository-administrator session for PR, release, ruleset,
runner, and emergency merge operations. Enable supported Actions usage alerts and
reporting but set no spending limit and no stop-usage budget.

Remove the former custom LiNKtrend GitHub App and all owned legacy state from IDE
Development and all nine consumers, including LiNKdeveloper: App workflows and
code, App IDs/private-key secret and variable names, installation repository
access, obsolete required statuses, self-hosted/Mac runner labels and
registrations, temporary runner creation, Mac coordinator/launchd services, and
owned Docker runner containers/images/volumes/networks. Never reveal secret
values, delete an ambiguous resource, or remove GitHub's normal built-in Actions
integration. Preserve Bugbot. Preserve all repository-owned required checks by
unioning them with stable managed checks. Preserve the separate concurrent
feature and all unique or dirty branch/worktree work.

For this permanent release only, use Carlos's approved repository-administrator
emergency authority exactly as documented: merge only exact verified PR heads,
prefer per-merge admin bypass without disabling rulesets, snapshot before state,
restore any unavoidable narrow ruleset change immediately, and prove protections
afterward. Promote the exact IDE Development release through `development` ->
`staging` -> `main`, tag and publish it, then install only that exact immutable
release into the nine consumers: `openclaw_prime`, `LiNKplatform`, `LiNKskills`,
`LiNKbrain`, `LiNKsites`, `LiNKdeveloper`, `LiNKlibraries`, `LiNKautowork`, and
`LiNKtrading-codebase`. Run the three Wave 3 consumer packets in parallel, their
repositories serially, opening one managed-system PR per consumer into
`development`, admin-merging the exact verified head, and promoting through
`staging` and `main`. Change managed IDE Development files in consumers but do not
change product/business code. LiNKdeveloper receives the package and App removal;
its autonomous VPS ProductRun redesign remains a separate next feature.

Finish only when IDE Development and all nine consumer remote `main` branches
contain the corrected system, all required protections are active, no owned former
App/self-hosted/Mac runner state remains, checkpoint/no-CI and hosted ARM64 flows
are proven, receipt reuse and changed-content rejection are proven, alerts are
enabled without a limit, and the final PASS report provides every packet attempt,
accepted SHA, source/consumer PR URL, merge SHA/tree, release and manifest digest,
test/Bugbot/run evidence, actual Actions usage, external cleanup evidence,
concurrent/product-code preservation evidence, and rollback references. Continue
end to end without stopping for ordinary repair, merge, promotion, or rollout
steps already authorized; stop only for a genuinely destructive ambiguity,
unavailable paid compute after two confirmed attempts, possible loss of unique
work, credential authority not present, or a scope decision that Carlos has not
already approved.

