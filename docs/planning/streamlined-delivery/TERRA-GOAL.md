# Copy-ready Terra Medium Goal

Use the following text for the new Terra Medium `/goal` task after Carlos approves it.

```text
Implement and release the IDE Development Streamlined Delivery feature in `/Users/linktrend/Projects/IDE Development`. The approved execution documents are on remote branch `issue/212-document-streamlined-delivery-implementation-pla`, not yet on `development`. Begin by fetching that branch, recording its exact remote tip as planning authority `P0`, and verifying that `P0` contains the complete non-empty directory `docs/planning/streamlined-delivery/` with 12 Markdown files and the seven packet files listed by its `README.md`. Use the existing planning worktree at `/Users/linktrend/Projects/IDE Development/.git/linktrend-worktrees/issue-212-document-streamlined-delivery-implementation-pla` if it is present, clean, and exactly matches `P0`; otherwise create a new read-only planning worktree at `P0`. Do not start implementation until you have read, in order, `README.md`, `IMPLEMENTATION-PLAN.md`, `FROZEN-INTERFACES.md`, `TERRA-ORCHESTRATOR-RUNBOOK.md`, `TERRA-GOAL.md`, and all seven files under `packets/`. Treat those documents at `P0` as the binding instructions for this goal and record any contradiction before dispatching an executor.

After loading the instructions, perform the runbook preflight against the main repository: read the root `AGENTS.md`, fetch and inventory all remote branches, worktrees, open PRs, rulesets, installed runner/coordinator services, active containers, and the completely separate concurrent feature. Record the then-current `origin/development` as immutable implementation base `B0`; `P0` is the planning authority and `B0` is the code base, so do not mistake the planning branch for the implementation base. Create `phase/streamlined-delivery` from `B0`. Preserve the planning documents in the final Phase history by integrating only the planning-document changes from `P0`, without importing unrelated planning-branch history or changing `B0`'s application state. Before Wave 1 dispatch, publish a preflight record containing `P0`, `B0`, the Phase branch SHA, concurrent-feature inventory, packet ownership-overlap result, and verified Luna/Codex configuration. Stop and report if the documents are missing, the planning worktree is dirty or does not match `P0`, the root is dirty, the Luna model cannot be verified, or packet ownership overlaps unresolved concurrent work.

Act as the sole Terra Medium orchestrator, integrator, and factual work verifier. Dispatch packet implementation only through Codex CLI to the verified GPT-5.6 Luna model with high reasoning; never use Cursor CLI. Create one isolated `issue/*` branch/worktree per packet from the correct wave base, provide each executor its exact packet plus `FROZEN-INTERFACES.md` and the immutable base SHA, use `phase/streamlined-delivery` as the only integration branch, execute dependency-independent packets concurrently within each of the three waves, verify actual diffs, owned paths, positive tests, negative probes, evidence, clean state, and exact pushed SHA before serial integration, and allow each Luna executor no more than two attempts. After a second failed Luna attempt, stop that executor and personally complete only that packet while continuing to oversee unaffected packets.

Deliver one Phase PR that aggregates independently verified Issue commits; ordinary checkpoints must create no PR and invoke no Bugbot. Implement the bounded Mac Mini coordinator as the primary scheduler, IDE-wide configurable fast/full/release profiles, exact Git-tree and dependency receipts, automatic obsolete-job cancellation, two attempts per exact candidate, no more than two sealed candidate revisions, durable stop-and-alert behavior, one heavy and at most two fast jobs subject to host-pressure limits, scoped container/worktree cleanup, automatic staging promotion, configurable automatic or principal-approved main promotion, normal GitHub credentials without the former custom LiNKtrend GitHub App, and one Cursor Bugbot review on the final sealed candidate. Preserve explicit `issue-pr` and GitHub-Actions compatibility profiles while making Phase integration plus local coordination the recommended new-install profile.

A separate feature is being developed concurrently in this repository. Do not modify, delete, reset, rename, rebase, force-push, absorb, or clean its branches, worktrees, commits, or PR. Keep packet ownership disjoint, detect overlaps at every wave boundary, synchronize the final Phase branch with the latest `development` without discarding either feature, rerun affected tests, and use only a short release lock for final promotion.

Override the current IDE Development Packager, Integrator, and scheduled promotion machinery for this feature as specified in the runbook: Luna agents checkpoint and push only; you create exactly one final Phase PR and perform normal-authenticated integration/promotion after required tests and Bugbot pass. If an obsolete current-system status context is the sole blocker to an otherwise proven merge, you may temporarily disable only that exact target-branch rule and must immediately install and verify the replacement protection. Never bypass failing tests, Bugbot, conflicts, missing evidence, or uncertain content identity.

Complete only after all packet and combined regression suites pass, a disposable consumer proves packaging, a live IDE Development canary proves timing/resource/restart/cancellation/two-attempt behavior, the exact release is promoted through development to staging to main, protections are active, the release is tagged and published, only this feature's temporary resources are removed, and the final report contains all packet attempts and SHAs, PR links, merge SHAs, tree/receipt identities, tests, timing, resource use, container cleanup, service version, ruleset evidence, concurrent-feature preservation, and rollback instructions. Do not roll this release into the nine consumer repositories; that is a separate goal.
```
