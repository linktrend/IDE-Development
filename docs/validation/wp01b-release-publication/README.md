# WP-01B managed-core release publication evidence

Machine-readable locator for WP-07 is `managed-core-release-evidence.json`.

- Rebuild is from final promoted packaging identity **v2.1.0**, never the pre-correction RC checksums.
- `publicationStatus` remains `pending_governed_publish` until the App-backed publisher runs on `main` tip after governed promotion.
- This directory records dry-run rebuild/verify evidence only. No tag/Release was created by the implementer.
