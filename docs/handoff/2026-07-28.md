# Handoff — 2026-07-28 (PR #19 App-token + dual-gate wake)

## Summary

Fourth correction pass on PR #19 after head `f9f334e`. Addresses App-token validation, same-job minting, dual Actions gate wake, readiness token wiring, and trailing whitespace.

## Finding → correction → proof → remaining external

| Finding | Correction | Proof | Remaining external |
|---------|------------|-------|--------------------|
| resolve required private key in consumer steps | Accept App ID + minted `LINKTREND_APP_TOKEN`; reject private-key leakage | behavioral test 13 | Carlos configures App |
| Staging/main minted token via job outputs | Single-job mint+consume; no `needs.*.outputs` token | static test “same-job only” | — |
| Wake only on `CI` | `workflow_run.workflows: [CI, Branch Source Policy]`; FAST_GATE includes both checks | behavioral test 12 exact order | Consumers must substitute static workflow-name list |
| readiness ignored AUTOMATION_TOKEN | `_gh_token()` prefers AUTOMATION_TOKEN | behavioral test 15 | — |
| trailing whitespace in App credentials doc | Removed; `git diff --check origin/development...HEAD` | command exit 0 | — |

## Do not

- Self-merge
- Post `cursor review`
- Edit consumers / Lisa / OpenClaw
- Create App/secrets/settings
