# Handoff — 2026-07-28 (PR #19 second correction pass)

## Summary

Second correction pass on PR #19 after live CI failure (run 30340902379) and independent review.

## Key fixes

1. Pull uses isolated worktrees; preserves caller checkout/branch/status/worktrees.
2. Readiness is GitHub commit status `Linktrend Review Ready` (no in-diff file/marker commit).
3. Packager split: discover (draft only) vs evaluate (per PR/check, race-safe head rereads).
4. Promotion split: `build` vs `reevaluate`/`repair-resume` (reevaluate never rebuilds/pushes).
5. Durable conflict tasks via GitHub Issues (file backend for tests); `--increment-attempt` wired.
6. Main approve-merge requires both expected SHAs.
7. Cleanup handles squash evidence, session ownership, promote branches; dry-run default.
8. Bugbot mention-only documented; consumer rollout blocked until confirmed.

## Tests

- `scripts/tests/test-gitops-behavioral.sh`
- `scripts/tests/test-gitops-review-packager.sh`
- `SKIP_LOCAL_ARCHIVE_CHECKS=1 scripts/verify-ide-development.sh`
- `git diff --check` (must exit 0)

## Bugbot

No new `cursor review` comment. Automatic usage-limit comments on prior heads prove mention-only is not active — see `docs/contracts/BUGBOT-MENTION-ONLY.md`.

## Do not

- Self-merge
- Consumer / Lisa / OpenClaw edits
- Claim complete while Verify IDE Development or Branch Source Policy is red (Bugbot may remain unavailable)
