# Handoff — 2026-07-28 (PR #19 concurrency + event filters)

## Summary

Fifth correction pass on PR #19 after head `f957fd0`. Preserves prior App-token / dual-gate / readiness fixes.

## Finding → correction → proof → remaining external

| Finding | Correction | Proof | Remaining external |
|---------|------------|-------|--------------------|
| Packager concurrency split by event/run/check id | Group by PR number else head SHA; `cancel-in-progress: false` | Static concurrency assert + dual-process race (exactly one Bugbot) | — |
| Integrator cancel-in-progress true during merge | `cancel-in-progress: false`; same PR/head key | Static test | — |
| Promote/Packager/Integrator minted tokens for unrelated CI | Job `if` filters on base/head/`workflow_run.event`/`head_branch` before checkout/mint | Event-filter matrix (22 rows) | — |

## Concurrency mapping

| Event | Packager / Integrator group key |
|-------|----------------------------------|
| `pull_request_target` | `pull_request.number` |
| `workflow_run` | `workflow_run.pull_requests[0].number` else `head_sha` |
| `check_run` | `check_run.pull_requests[0].number` else `head_sha` |

Promotion keeps deterministic groups `development-to-staging` / `staging-to-main`.

## Do not

- Self-merge / post `cursor review` / edit consumers / Lisa / OpenClaw
- Create App/secrets/settings
