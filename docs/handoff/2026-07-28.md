# Handoff — 2026-07-28 (PR #19 uniform SHA concurrency + trusted resolve)

## Summary

Sixth correction pass on PR #19 after head `d61c2ad`. Preserves prior App-token / dual-gate / readiness / event-filter fixes.

## Finding → correction → proof → remaining external

| Finding | Correction | Proof | Remaining external |
|---------|------------|-------|--------------------|
| Concurrency mixed PR number vs SHA across event shapes | Uniform candidate head SHA for all automatic events; `cancel-in-progress: false` | Parse live/managed concurrency expressions; same SHA → same group for prt/wr/cr ± empty PR arrays | — |
| Test-only `bugbot_request_once.py` flock false proof; test-only `event_relevance.py` | Deleted both; tests use real workflow YAML + `packager_logic.should_request_bugbot` / `resolve_event_pr.resolve_candidate` | Helpers absent; managed≡live byte-identical; serialized comment-reread → `skipped_duplicate_marker` | — |
| Workflow `if` required `pull_requests[0]` while code claimed SHA fallback | Trusted read-only `resolve` job (`GITHUB_TOKEN` + `resolve_event_pr.py`); App mint only when `relevant=true` | Resolver matrix: empty PR arrays, fork, no match, ambiguous, development/staging/main promote | Carlos: App + secrets still external |

## Uniform concurrency identity

| Event | Packager group suffix | Integrator group suffix |
|-------|----------------------|-------------------------|
| `pull_request_target` | `pull_request.head.sha` | same |
| `workflow_run` (PR array empty or not) | `workflow_run.head_sha` | same |
| `check_run` (PR array empty or not) | `check_run.head_sha` | same |
| `workflow_dispatch` | `misc-{run_id}` (no SHA) | `inputs.pr_number` else `misc-{run_id}` |

Expressions:

- Packager: `linktrend-packager-eval-${{ github.event.pull_request.head.sha \|\| github.event.workflow_run.head_sha \|\| github.event.check_run.head_sha \|\| format('misc-{0}', github.run_id) }}`
- Integrator: `linktrend-integrator-eval-${{ github.event.pull_request.head.sha \|\| github.event.workflow_run.head_sha \|\| github.event.check_run.head_sha \|\| github.event.inputs.pr_number \|\| format('misc-{0}', github.run_id) }}`

Cross-run serialization = GitHub Actions concurrency. Idempotency = reread GitHub PR comments after acquiring the slot (`should_request_bugbot` → `skipped_duplicate_marker`). Local flock is not used and does not prove Actions concurrency.

## Empty `pull_requests` / forks

- Preferred policy implemented: read-only resolve job → commits/pulls API when event array empty.
- Fail closed on zero or ambiguous matches (no App mint).
- Fork-backed candidates: supported when exactly one matching open PR resolves; privileged jobs still checkout default branch only.
- Push CI / agent tips with no matching open PR: not evaluated (honest unsupported until a PR exists). Does not require Cursor Cloud changes; Cloud agent PRs with empty event arrays still resolve via API when the PR exists.

Promotion keeps groups `development-to-staging` / `staging-to-main`.

## Do not

- Self-merge / post `cursor review` / edit consumers / Lisa / OpenClaw
- Create App/secrets/settings
- Open another PR
