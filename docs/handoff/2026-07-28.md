# Handoff — 2026-07-28 (PR #19 trust-boundary correction)

## Summary

Third correction pass on PR #19 addressing independent-review merge blockers after head `27ce1d1`.

## Finding → correction → proof → remaining external requirement

| Finding | Correction | Proof | Remaining external |
|---------|------------|-------|--------------------|
| Privileged workflows executed PR-controlled code with write perms | Privileged jobs use `pull_request_target` / `workflow_run` / `check_run` and checkout **default branch only** with `persist-credentials: false`; CI stays `contents: read` | `scripts/tests/test-gitops-review-packager.sh` trust-boundary section; managed workflows | Workflows only activate from default branch after merge |
| Actions gate completion does not wake via `check_run` | Packager/Integrator/promote listen to `workflow_run` on `CI`; external `check_run` retained for Bugbot | Static wake assertions + behavioral scenario 12; `CI-GATE-CONTRACTS.md` consumer mapping | Consumers must map their gate workflow **name** into `workflow_run.workflows` |
| `GITHUB_TOKEN`-created PRs need manual workflow approval | Fail-closed GitHub App token contract (`LINKTREND_GITOPS_APP_ID` / `LINKTREND_GITOPS_APP_PRIVATE_KEY`); outcome `automation_credentials_blocked` | `docs/contracts/GITHUB-APP-GITOPS-CREDENTIALS.md`; credential tests | Carlos must create App + secrets (agents must not) |
| Staging reevaluate followed current development tip | Exact `PROMOTE_PR_NUMBER` + `EXPECTED_PROMOTE_HEAD`; machine-readable `<!-- linktrend-promote: {...} -->`; target advance invalidates; fetch fail-closed | Behavioral scenarios 3/11; promote scripts | Live promote smoke after App configured |
| Packager overwrote PR title/body | Managed section `<!-- linktrend-packager:begin/end -->` only; ready PRs untouched | Behavioral scenario 10 | — |
| Green Packager job meant only script success | Honest outcomes: packaged/waiting/skipped/blocked/failed/bugbot_requested/merged/automation_credentials_blocked + result checks | `write_outcome.py` + workflow summaries | Operators must read outcome, not job green alone |
| Bugbot auto usage-limit comments | No new `cursor review`; mention-only remains required gate; consumer rollout blocked | `BUGBOT-MENTION-ONLY.md`; this handoff | Confirm `manualTriggerOnly` per repo; default-branch smoke |

## Event wake chain

```text
Work tip + Review Ready status
        │
        ▼
Packager discover (schedule) ──draft PR──► pull_request_target (Packager evaluate once)
        │                                         │
        │                                         ▼
        │                              CI workflow_run (success) ──► Packager reevaluate
        │                                         │
        │                    gates green + ready on exact SHA ──► Bugbot request (once)
        │                                         │
        │                              external check_run (Bugbot)
        │                                         ▼
        │                              Integrator (workflow_run / check_run / PR)
        │                                         │
        │                                      merge → development
        ▼
Staging build (schedule) → promote/staging/* PR → CI workflow_run → exact reevaluate → merge staging
        ▼
Main package (schedule) → promote/main/* PR → Approve binds staging+main+head → merge main
```

## Tests run

- `bash scripts/tests/test-gitops-review-packager.sh`
- `bash scripts/tests/test-gitops-behavioral.sh`
- `git diff --check`
- YAML parse of all managed + live workflows
- `SKIP_LOCAL_ARCHIVE_CHECKS=1 bash scripts/verify-ide-development.sh` (full)

## Do not

- Self-merge PR #19
- Post another `cursor review`
- Edit consumers / Lisa / OpenClaw
- Create GitHub App, secrets, or repo settings
- Roll out to consumers until default-branch smoke + mention-only confirmed + App configured
