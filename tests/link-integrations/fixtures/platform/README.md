# LiNKplatform consumer fixtures (WP-I6-S1)

Local synthetic AuthClaims `1.1.0` objects only. Identifiers are fixture
labels, not live credentials. Do not mint claims, store signing keys, call
Platform HTTP, or add runtime secrets.
