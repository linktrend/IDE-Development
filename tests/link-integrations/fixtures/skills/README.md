# LiNKskills consumer fixtures (WP-I6-S4)

Local synthetic release and completed-use telemetry objects only. Provider
commit/tree match the S0 LiNKskills pin. Digests and opaque refs are fixture
labels, not live payloads. Do not execute a skill, call Skills HTTP, edit
LiNKskills, add credentials or network adapters, or install nested
`.ide-development/`.
